<%=
  live_render(@conn, ILikeTrainsWeb.Live.Game,
    session: %{
    },
    container: {:div, [class: "game"]}
  )
%>

<%#
  live_render(@conn, ILikeTrainsWeb.Live.Game,
    session: %{
      game_id: @game.id,
      game: @game,
      player_id: @player.id,
    },
    container: {:div, [class: "game"]}
  )
%> 